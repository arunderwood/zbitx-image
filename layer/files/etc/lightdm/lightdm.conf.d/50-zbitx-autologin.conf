# Auto-login the `pi` user at boot so the sBitx.desktop XDG autostart
# entry fires without the user having to type a password.
# This is the same mechanism `raspi-config` uses for its "Desktop
# with autologin" mode (B4).
[Seat:*]
autologin-user=pi
autologin-user-timeout=0
